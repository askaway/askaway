.navbar.navbar-inverse.navbar-fixed-top{:role => "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => "/"} Ask Away
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li 
          = link_to 'Newest', new_questions_path
      = link_to 'Ask a question', new_question_path, class: 'btn btn-primary navbar-right navbar-btn navbar-btn-ask'
      %ul.nav.navbar-nav.navbar-right
        %li 
          - if current_user
            = link_to 'Logout', destroy_user_session_path, method: :delete
          - else
            = link_to 'Login', new_user_session_path

- flash.each do |name, msg|
  - flash_class = 'alert'
  - flash_class += ' alert-success' if name == :notice
  - flash_class += ' alert-warning' if name == :alert
  %div{class: flash_class}
    %button{type: 'button', class: 'close', :'data-dismiss' => 'alert', :'aria-hidden' => 'true'} &times;
    .container
      %div= msg

