%div
  %br/
  %h4 Question of the day:
  .row-fluid
    #QOTD.span9
      %h2= featured_question.body
      %p.muted
        =asked_by(featured_question)
      =facebook_button(featured_question)

      %br/
      %h4 Answers from the candidates:
      .row-fluid
        .candidate-picker
          - featured_question.answers.each do |a|
            %a.span2.candidate{id: "candidate-picker-#{a.candidate.id}", data: {toggle: :tab}, href: "\##{last_name_from_answer(a).downcase}"}
              = image_tag a.candidate_avatar
          - featured_question.candidates_with_no_answer.each do |c|
            .span2.candidate.candidate-with-no-answer{id: "candidate-picker-#{c.id}"}
              = image_tag c.avatar
      .tab-content
        -featured_question.answers.each_with_index do |a, i|
          =render partial: 'questions/candidate_answer', object: a, locals: {index: i}

    = render 'questions/recently_asked_sidebar'
  .inline-message
    = link_to "See all the questions", questions_path, role: "button", class: "btn btn-primary btn-large"
    Or, if you've got your own question
    %a.btn.btn-ask.btn-large{"data-toggle" => "modal", :href => "#ask-modal", :role => "button"} Ask a Question
