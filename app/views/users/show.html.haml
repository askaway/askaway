.list-group
  .list-group-item
    %div.media.comment.answer
      %span.pull-left
        %img.media-object.comment-avatar{src: @user.avatar_url}
      .media-body
        %h1.list-group-item-heading.user-title= @user.name

- if @user.questions.count > 0
  %ul.list-group
    %li.list-group-item
      %h2.user-title.list-group-item-heading Questions
    - @user.questions.not_anonymous.each do |question|
      %li.list-group-item= link_to question.body, question
