.row
  .col-xs-12.col-md-8.col-md-offset-2
    .panel.panel-default
      .panel-heading
        %h1.panel-title= @title
      .panel-body
        = simple_form_for @resource, :url => upload_avatar_resource_path(@resource), :html => { :multipart => true } do |f|
          = f.file_field :uploaded_avatar
          %br
          = f.submit 'Upload', class: 'btn btn-primary', data: {disable_with: 'Uploading...'}
